services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.6.0
    ports:
      - "9000:8080"
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
    # volumes:
    #   - ./kc-providers/jars:/opt/keycloak/providers/
    command:
      - start-dev